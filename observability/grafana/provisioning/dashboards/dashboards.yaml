apiVersion: 1

providers:
  - name: "FastAPI"
    orgId: 1
    folder: "FastAPI"
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards
